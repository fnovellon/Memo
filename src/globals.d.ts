/** Injectés au build par Vite — voir `define` dans vite.config.ts. */
declare const __APP_VERSION__: string;
/** Commit court, ou chaîne vide hors d'un dépôt git. */
declare const __APP_COMMIT__: string;
